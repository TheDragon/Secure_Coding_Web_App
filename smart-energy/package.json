{
  "name": "smart-energy",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix server i && npm --prefix client i",
    "dev": "npm --prefix server run dev & npm --prefix client run dev",
    "start": "npm --prefix server start",
    "build": "npm --prefix client run build"
  }
}
